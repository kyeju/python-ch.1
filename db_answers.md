# DB 과제 확인 질문

1. PRIMARY KEY는 각 데이터를 구분하기 위해 사용합니다. 중복된 값이나 NULL은 들어갈 수 없습니다.

2. WHERE 없이 UPDATE를 실행하면 모든 데이터가 수정되고, DELETE를 실행하면 모든 데이터가 삭제될 수 있습니다.

3. SELECT *는 모든 컬럼을 조회하고, 컬럼 이름을 적으면 필요한 컬럼만 조회합니다.

4. COUNT()는 데이터의 개수를 세고, AVG()는 숫자 데이터의 평균을 구합니다.

5. Python에서는 리스트의 데이터를 반복문과 조건문으로 처리했습니다. DB에서는 테이블에 데이터를 저장하고 SQL 조건을 사용해서 필요한 데이터를 조회했습니다.
